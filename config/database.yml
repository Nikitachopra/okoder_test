# SQLite version 3.x
#   gem install sqlite3
#
#   Ensure the SQLite 3 gem is defined in your Gemfile
#   gem 'sqlite3'
#

development:
  adapter: mysql2
  encoding: utf8
  database: okoder_dev
  pool: 5
  username: root
  password: root
  reconnect: true

  test:
  adapter: mysql2
  encoding: utf8
  database: okoder_dev
  pool: 5
  username: root
  password: root
  reconnect: true

# default: &default
#   adapter: sqlite3
#   pool: 5
#   timeout: 5000

# development:
#   <<: *default
#   database: 

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
# test:
#   <<: *default
#   database: okoder_test

# production:
#   <<: *default
#   database: okoder_prod
